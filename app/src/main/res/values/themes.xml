<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        No action bar.
        True black background.
        All surfaces black — no card elevation, no ripple colour.
    -->
    <!--
        DayNight parent + forced MODE_NIGHT_YES (in MinimalLauncherApp) means
        AppCompat dialogs (AlertDialog) automatically use dark surfaces,
        so showAppOptions() gets a dark dialog for free.
    -->
    <style name="Theme.MinimalLauncher" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Window -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@style/MinimalWindowAnimation</item>

        <!-- Edge-to-edge: bars are transparent, window background shows through -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Text -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/gray_hint</item>

        <!-- Colours used by Material components — keep everything black -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlHighlight">@color/press_highlight</item>
    </style>

    <!-- Minimal window transitions: simple fade, no slide/bounce -->
    <style name="MinimalWindowAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>
</resources>
